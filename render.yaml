      services:
      - type: web
        name: account-app
        env: node
        plan: free
        buildCommand: "npm ci && npm run build"
        startCommand: "npm start"
        autoDeploy: true
        region: singapore
        envVars:
          - key: NODE_ENV
            value: production
          - key: NODE_VERSION
            value: 20